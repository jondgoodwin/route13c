///////////////////////////////////////////////////////////////////////////////
//
// Cart
//
///////////////////////////////////////////////////////////////////////////////

#include "Cart.h"

CartId Cart::nextId = 0;

Cart::Cart(LocationId lastloc, int32_t cap, int32_t pay) :
    id(nextId++),
    lastKnownLocation(lastloc),
    capacity(cap),
    payload(pay)
{
};
