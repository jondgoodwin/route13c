///////////////////////////////////////////////////////////////////////////////
//
// Cart
//
///////////////////////////////////////////////////////////////////////////////

#include "Cart.h"

Cart::Cart(CartId cartId, LocationId lastloc, uint32_t cap, uint32_t pay) :
    id(cartId),
    lastKnownLocation(lastloc),
    capacity(cap),
    payload(pay)
{
};
